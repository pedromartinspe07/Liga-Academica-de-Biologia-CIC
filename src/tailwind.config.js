/** @type {import('tailwindcss').Config} */
module.exports = {
    // Configura os caminhos dos arquivos onde o Tailwind irá procurar por classes.
    // Certifique-se de que isso inclui todos os seus arquivos de componente React.
    content: [
      "./src/**/*.{js,jsx,ts,tsx}",
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  